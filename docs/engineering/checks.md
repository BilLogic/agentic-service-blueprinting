---
summary: Every guard in CI, what each one is defending and what its failure means — the identifier manifest, the four version statements, adapter parity, the generated portable core, the generated database types and the columns a deployment's own declaration has to describe, the reserved migration band, the IR round trip, the vendored-rulebook drift guard, the write and read surfaces, the RPC argument names, the seed that has to load on a fresh core and the deployment seed that is the parity substrate, the paths the plugin surface names, the paths a deployment imports, the dependency vocabulary, the database names inside string literals, the standalone sweep and the content-coupling sweep beside it, the advisory sweep that mirrors them at an adopter, the release tag, the docs index, the browser render walk over the sample board and the three checks that keep `AGENTS.md` a router.
---

# The guard set

**For** anyone whose pull request just went red, and anyone adding a guard.
**Answers** what is each check defending, and what does its failure mean?

Every check here started as a script somebody could remember to run. A guard
nobody runs is not a guard, so they all run on every pull request
(`.github/workflows/ci.yml`). Each one exists because the thing it checks was
once stated in prose and was once untrue.

## 1. The plugin contract

| Command | What goes red |
| --- | --- |
| `npm run check:manifest` | An identifier a consumer resolves at runtime changed. `identifiers.json` is generated by walking `skills/`, `references/`, `agents/` and `hooks/`; a rename shows up as a diff so the reviewer can ask who else says that word. Two reference files sharing a basename is a hard error, not a diff — `get_reference` resolves by bare name. |
| `npm run check:rpc-arguments` | A call in `src/lib/authoringRpc.ts` posts an argument the function does not have, or omits one that has no default. PostgREST resolves an RPC by matching the body's KEYS to a function's parameter names, so a stray key is `PGRST202 — could not find the function` rather than a null column: the write never lands, and `client.rpc` is called through an `any` cast, so nothing in TypeScript sees it. The parameter lists are parsed from `supabase/generated/portable-core.schema.sql`, and revert specs are swept with the calls because an inverse posts the same body one undo later. The `p_` prefix is compared verbatim — PostgREST strips nothing, and the `p_`-prefixed functions are called with the prefix. This is #168: `21000116000000` renamed `cell_dependencies.label` to `.name` and the template kept sending `label`, so every arrow saved from the panel failed for a release. |
| `npm run check:doc-paths` | A document this package packs — `skills/`, `references/`, `agents/`, `hooks/`, `docs/` or a document at the root — names a repository path that no longer exists. An installed skill opens the paths its documents write down, so a rename nobody carried into the prose hands the agent a dead end mid-run — this found `src/lib/mutations/` (a directory that never existed), `src/styles/tokens.css` and `scripts/generate_scale_fixture.mjs`. A token resolves from the root, from the document's own directory, or as a segment-aligned suffix of a tracked path, because the docs legitimately write `validate_ir.py` and `adapters/fixture.ts`. Paths that only exist in an ADOPTER's workspace are listed, with a reason each, in `WORKSPACE_ARTIFACTS`, whose entries are globs because that is how the documents write. The subject stopped at the plugin surface until it was measured: `docs/` is the largest prose tree here and nothing resolved the paths it named, nor did anything resolve the root documents', and planted dangling paths in `CONTEXT.md` and this file passed the whole suite. Widening it reported fifteen sites: one real rename nobody carried into the prose, three the workspace glob already covered and an exact-key lookup could not match, four in decision records, and seven — six sentences — that are right about a file this tree is right not to have. Those seven are `ABSENT_BY_DESIGN`, keyed by the document AND the token, so an exemption cannot travel to the document where the same path is a defect. `CHANGELOG.md` and the decision records are out, both on the same ground: they keep the words of the day they were written, and thirty-eight of their paths were true then. The boundary with the citation guard in `src/citations.ts` is unmoved — this check REQUIRES a path to resolve here, that one FORBIDS a shared file naming a path its reader lacks, and the line is which reader the document is written for. |
| `npm run check:harness` | An assembled file is documented by no composition document, by two of them, or a document claims a file that is no longer there. The documents under `docs/guidelines/composition/` each carry a `claims:` list, and the check holds those lists against the files under `src/components/{blueprint,editor,cover,mobile}` in both directions, so a surface nobody documented turns the build red here rather than rotting quietly. The mapping is declared rather than derived from folder names because folder names are not stable — `editor/` alone spans the canvas, the sidebar, slices, the agent and the dialogs, and the `layer`→`lane` rename is the standing proof. A DEPLOYMENT holds the script byte-identical and runs it over both folders: this package's documents, out of the package it installed, plus its own. That is what makes a pin bump free — the claim for a file is written where the file lives, so a release that adds thirty-one modules adds thirty-one claims here and none there, while a file the deployment assembles under a tree its `composition.claimed` names still needs a claim of its own. `references/customization.md` § Composition claims is the deployment-facing half. |
| `npm run check:reference-paths` | A path a DEPLOYMENT imports from this repo by fixed name is gone. The deployment this template was generalised from installs it as a git-URL dependency pinned to a tag and imports eighteen references and the four `SKILL.md` bodies as build-time module specifiers, so a move here used to land green and surface at the consumer's build one pin later — and the file-sync that preceded the pin was deleted for inverting a rename across eighteen files. `CONSUMER_IMPORTS` in `scripts/check-reference-paths.mjs` holds the list; each path must be both present on disk and tracked, because the index still says the old name after a bare `mv`. Moving one is a release, not a refactor: docs/adr/0004-reference-paths-are-a-published-interface.md. |
| `npm run check:dependency-kinds` | A rulebook document teaches a `cell_dependencies.kind` value the CHECK constraint refuses. The Enums row in `references/data-model.md` is compared against the constraint in `supabase/generated/portable-core.generated.sql`, and the retired kind words are banned outright from the documents that repeat a value, because those have no enum row to compare against. Twice over: in their code-span form from `references/`, `skills/`, `agents/`, `docs/guide/` and `docs/engineering/`; and as bare words, in the short list of phrases where the two can only be dependency kinds ("trigger-vs-needs", the slashed pair, either word beside "edge"), across `references/`, `skills/`, `agents/` and `evals/` — markdown, JSON and Python alike. The second sweep exists because two documents spelled the pair without backticks and stayed green for a release. A database trigger is out of reach of those phrases rather than exempted; `BARE_ALLOWED` holds the two sentence kinds that are exempted, with a reason each. |
| `npm test` | The vendored rulebook drifted, or the app suite failed. `npm test` runs `sync-canvas-skills.mjs --check` before Vitest: `skills/` + `references/` are canonical and `src/lib/agent/skill/` is a copy the app bundles. Fix the source, then `npm run sync:canvas-skills`. |
| `npm run slice:annotation-drag` | The annotation-drag flow broke somewhere between the pointer and the cells a mark covers. The slice (`src/slices/annotationDrag.slice.test.tsx`) opens annotation mode from the real `CanvasAnnotationToolbar`, draws a box across two cells of a board through the real `CanvasAnnotationLayer` — the real pointer sequence, the real `clientToLocal` camera un-projection, the real frame-batched drag queue — drags it onto a third cell, and reads it back through the real `AnnotationCaptureMenu` over the real `captureMarks`: the captured payload names the cells the box covers, the two it was drawn over before the drag and the one it was dragged onto after it, and taking the layer off the page and mounting it again under the same provider leaves the mark where the drag left it. There is no persistence; the read-back is the capture, and the issue's word was wrong: annotations are deliberately never persisted (`src/lib/annotationCapture.ts`), nothing is stored anywhere, and the capture — `setPendingAgentAttachment`, an in-memory hand-off to the composer — is the one thing the flow produces, which is also the answer a broken split would get wrong. What is stubbed is geometry at the smallest seam — `getBoundingClientRect` on the layer and on each cell, `offsetWidth`/`offsetHeight` on the layer, and `setPointerCapture`/`releasePointerCapture`, none of which jsdom provides — and one leaf read, the Supabase provider's `canWrite`. The stubbed camera is deliberately not zoom 1, so a split that dropped the scale term out of the un-projection goes red rather than dividing by one, and frames are faked and turned by hand so the drag queue is watched publishing mid-gesture rather than only at the `pointerup` flush. Two cases watch it fail: one drops the drag's position write, one moves the pointer under `DRAG_THRESHOLD` and requires the mark not to move. The three stubs are the three things jsdom cannot do at all — layout, the CSS-transform camera, pointer capture — and they are covered in a browser by `render-walk/annotation-drag.spec.ts`, under `npm run check:render-walk`. It is the second flow the large-component-splits decision marks covered, and it unblocks `src/components/editor/CanvasAnnotationLayer.tsx`'s split; it also runs inside `npm test`, and is named as its own step so a red run says which flow. |
| `npm run slice:cell-edit` | The cell-edit-with-revert flow broke somewhere between the panel and the row. The slice (`src/slices/cellEditRevert.slice.test.tsx`) edits a cell from the real panel over the real cell-detail provider, saves through the one save and the real content and spec mutations, reverts each entry from the real change sheet through the real revert, and reads the row back column for column against an in-memory table (`src/test/inMemoryDatabase.ts`), with the panel's two leaf reads stubbed. A second case proves it goes red when a written column does not land. It is the per-flow exit condition the large-component-splits decision now names, and the first flow it marks covered; it also runs inside `npm test`, and is named as its own step so a red run says which flow. |
| `npm run slice:cell-edit:postgrest` | The same flow, over a REAL database, would not save or would not read back — a grant or a policy the recipe forgot on a column the panel writes, which the in-memory table above cannot see. `scripts/run-slice-over-postgrest.mjs` builds the stack `check:seed-load` builds into a scratch database, starts a pinned PostgREST release on it, mints the service claim (`scripts/mint-service-claim.mjs` — the `app_metadata.role = 'service'` the recipe's restrictive policies read) and runs the same slice file with `SLICE_POSTGREST_URL` and `SLICE_POSTGREST_JWT` set, so `src/test/postgrestDatabase.ts` stands in for the in-memory table; the row it edits is one the seed holds. `--prove` then revokes UPDATE on one written column, requires the slice to go red, and puts the grant back — the guard has been watched fail. It prints its runtime, because the decision to keep this the primary form is a decision about that number; the first CI run measured 3.9 seconds for the slice and 4.4 for the proof. Runs in the `portable-core` job, after the seed-load check; without a PostgREST binary it says so through the unverified register and stops — which is what it does on this repository's own machines, so CI is where it has run end to end. |
| `npm run slice:agent-session` | The agent-session flow broke somewhere between the composer and the row. The slice (`src/slices/agentSession.slice.test.tsx`) opens the real `AgentPanel`, starts a session, sends a message, and runs the real loop against the scripted provider adapter #694 introduced — no network, no key — which calls one read tool (`get_cell`) and then one write tool (`update_cell`) over the real definitions, the real one save and the real content and spec mutations. It asserts what a person sees and what the database holds: the turn, each tool call and the result its row discloses; the row itself, both halves, nothing else moved; one ledger entry per write path, each wearing THIS session's agent attribution, and a ✦ per row in the real change sheet; the original back, column for column, after both reverts from that sheet; and the transcript read back out of the persisted `agent_messages` rows after the panel is closed and reopened — the in-process run is forgotten between the two, so the reopen hydrates from the database as a session reopened in another browser would, and what it asserts is what those rows carry (the message, the narrations, the answer, the tool names) and what they do not (the tool row's stripped-out arguments and result). Two further cases prove it goes red: one drops a written column while the tool still reports success, the other runs the write with the session's attribution stripped from the live tool context, and the ledger's author, session id and ✦ go with it. The database is the same in-memory table the cell-edit slice uses (`src/test/inMemoryDatabase.ts`), which cannot see a grant or a policy; only the Supabase provider, the provider adapter and the viewport probe are mocked. It is the per-flow exit condition the large-component-splits decision names for `src/components/editor/AgentPanel.tsx`; it also runs inside `npm test`, and is named as its own step so a red run says which flow. |

## 2. The version, in its four statements

| Command | What goes red |
| --- | --- |
| `npm run check:version` | `package.json`, `.claude-plugin/plugin.json` and the CHANGELOG's top heading state different versions. `package.json` is the source; `--write` propagates into `plugin.json`. The CHANGELOG entry stays a human's job, so a release with nothing written still fails. |
| `npm run check:release-tag` | A `v*` tag names a version no CHANGELOG release does, or a tag's tree states a different version. The tag is the only thing a consumer can pin. `--require` — that *this* version has a tag — is the release step, not the pull-request step, because the tag is cut after the release commit is on `main`. CI must `git fetch --tags` first: a checkout does not fetch them. |

Procedure: [releasing.md](./releasing.md).

## 3. The database, and the portability claim

| Command | What goes red |
| --- | --- |
| `npm run check:portable-core` | The two generated halves no longer describe the migrations. `supabase/generated/` is emitted from `-- @core` / `-- @recipe` marks in the migration SQL; a hand edit is reverted by this diff. Run `npm run generate:portable-core`. |
| `npm run check:portable-schema` | `supabase/generated/portable-core.schema.sql` is not what the series builds. A migration changed the schema and the dump — `pg_dump` of a stock Postgres after the core, with only the names a backend ends up holding — was not regenerated. It is the file the vocabulary checks read, so it has to be current. Run `npm run generate:portable-schema`. |
| `npm run check:database-types` | The committed `src/types/database.ts` is not what the built core generates — a migration changed a shape the types do not carry, or the file was edited by hand. The whole file is generated: `scripts/generate-database-types.mjs` stands up the portable core and the recipe on a fresh Postgres and runs the engine behind `supabase gen types` over it, so there is no linked project and no hand-written half to preserve. `--check` regenerates into memory and names the first line that differs. Run `npm run generate:database-types` and commit the result. The tail is DERIVED in the same run: each enum union is read off the domain or column CHECK that closes the vocabulary, so a value added to a constraint reaches the type without anybody remembering — and the schema-inventory step below now covers those unions too, member for member in both directions, so a union that drifted from its constraint fails there as well. Needs a reachable Postgres and permission to create a database; in CI it is the `portable-core` job's service. |
| `npm run check:instance-vocabulary` | A name the instance built on this template has retired is still live here — a table, a column, a value a CHECK accepts. The instance's rename map is read from its repository (the sibling checkout when there is one, GitHub otherwise) and held against the schema dump, so a rename made there fails a build here rather than waiting to be noticed. A divergence this template keeps on purpose is listed in `ACCEPTED_DIVERGENCES` with the issue that will remove it, and an entry nothing matches any more is itself a failure. |
| `npm test` → `portable-schema.test.mjs` | The schema dump carries an identifier with a retired fragment in it — the usual cause is a rename that reached the table and not its constraint or index. |
| `npm run check:band` | A new upstream migration was stamped with the wall clock instead of allocated from the reserved band. Timestamp order is apply order, so an upstream file stamped below a fork's own migration lands before it and desyncs them. |
| `npm run check:parity` | The SQL adapter and the no-database adapter project different field sets out of one blueprint. The contract calls the second one "not a degraded mode"; this is what makes that sentence true. |
| `npm run check:seed-load` | The generated seed does not load onto a fresh core + recipe, or it loads but the **anon** key cannot read the content back. Stands up a database of its own (shim, the platform's SELECT default, core, recipe, seed), then reads every seeded table and the two render joins as `anon` — the role the deployed app's key resolves to. Every other database check reads as the owner, which cannot see the one failure the deployer feels: a table the recipe never exposed renders blank in the browser and stays green everywhere else. It then asks the other half — what a signed-in **author** may write — by becoming that role: for every column and verb the panels write, it sets `authenticated`, sets a service-account claim, attempts the write and rolls it back, then attempts the same write as a signed-in reader, who must change nothing. Both directions matter. A grant or a policy has gone missing in three of the last migrations, and a policy that refuses matches zero rows and returns 200, so the catalogue cannot tell "an author may write this" from "only a service account may". Needs a reachable Postgres and permission to create a database; in CI it is the `portable-core` job's service. |
| `npm run check:function-bodies` | A `language sql` function in `public` does not resolve when it is CALLED. A SQL body is stored as text and resolved at call time, so a rename moves the relation and leaves every body naming it untouched: the function keeps existing, keeps dumping cleanly, and raises `42P01` the first time anybody calls it. Creation is no defence — the body was valid when it was written, and the rename that falsified it validates nothing. This stands up the same stack as the row above, then calls every `language sql` function with a typed null per argument inside a rolled-back transaction, plus `slices_referencing` and `deletion_impact` with real ids out of the seed. Only the SQLSTATEs meaning "that is not there" fail it (undefined table, column, function, schema); a function raising its own exception on null input has already answered the question and is reported as tolerated. `--self-test` plants the defect in its own order — a table, a body that reads it, then the rename — and asserts the call is reported, because a sweep where everything answers looks identical to one that called nothing. This is #171: `21000115000000` renamed `slice_items` to `slides`, `slices_referencing` kept reading the old name, and since `deletion_impact` reads that function and every delete RPC reads `deletion_impact`, no structural delete could succeed on a fresh core. |
| `npm run check:deployment-seed-load` | *Not in CI, and by decision nothing schedules it — it needs a deployment's checkout beside this one; see § 3, the three live guards.* A **deployment's own** seed does not load onto this template's portable core, or loads and does not render to `anon`. Same stack as the row above with the last step swapped: the deployment's seed replaces this repository's, loaded in the order the deployment itself states under `[db.seed]` in its `supabase/config.toml`. This is the behavioural-parity substrate a reconciliation ticket reads against — the sibling row proves the loop closes on content this repo generated, which the generator and the schema can hardly disagree about; only a real deployment's content answers whether the core is *sufficient*. It applies the seed with `ON_ERROR_STOP` **off** on purpose, because here the failing statements are the deliverable rather than a bug to stop at: every one is collected, grouped by reason with counts and examples, and knock-on failures (a foreign key whose row an earlier failure never inserted, the core's own `cells: …` raises, an aborted transaction block) are listed separately so the one root cause is not buried under the forty it caused. Point it at a deployment with `-- --seed <path>` or `DEPLOYMENT_SEED=<path>`; with neither it looks for a checkout beside this one that ships a `supabase/seed.sql` and states a different package name, and **skips with a message** when there is none or more than one. A deployment whose `[db.seed]` is present but **disabled or empty** has taken its seed list out of the CLI's reach on purpose — `db push --include-seed` reads that table and defaults to `--linked` — so the list lives in a loader of its own and the check REFUSES rather than falling back to the one file it was pointed at. Name the files instead, in load order: `--seed a.sql --seed b.sql`, or `--seed a.sql,b.sql`. It used to fall back, which is how it once read one of a deployment's twenty-three seed files and reported every table the other twenty-two fill as one the anon key could not see. |
| `node scripts/check-database-types-superset.mjs <file>` | *Not in CI — it needs a deployment's own types file, which a deployment has and this repository does not; same stance as the three live guards below.* A deployment's `types/database.ts` does not describe a column this package's own generated types declare, on a table that deployment does have — or describes one so differently that the two files cannot be about the same column. That file is a deployment's DECLARATION of its database, not what its application compiles: the application is read out of this package and typechecked against the copy beside it. So the check asks what a declaration can be wrong about. A table this package describes and the deployment does not is a migration it never ran — printed, not failed, because a deployment is entitled to carry the part of this core its service uses. A column absent from a table it DID build is the finding: the application will read that column out of that database. Column types are compared for contradiction rather than width — `string` and a narrowed vocabulary are one text column at two precisions, `Json` and `NonNullable<Json>` one jsonb column disagreeing about null, and `string` against `number` is two files that cannot both be right. The unions in the file's tail are not compared at all: they are this package's own aliases, imported by this package's code out of this package's copy, no `Row` column is typed as one, and the Supabase CLI a deployment's docs send it to emits no such tail — held to them, an honest deployment failed for following its own instructions. It reuses the inventory check's parsers, so there is one answer to what that generated file means. The deployment runs it in its own CI, against its own file. |
| `npm run check:target` | *Not in CI — it needs a live project, which a deployment has and this repository does not; see § 3, the three live guards.* Asks the configured database for `public.schema_version` and distinguishes never migrated from stale from fine. Worth running once against any target, because the fallback renders perfectly over a database that was never migrated. |
| `npm run check:agent-account` | *Not in CI — it needs a live project, which a deployment has and this repository does not; see § 3, the three live guards.* A connected database's agent-facing account has drifted from `panelTerms.ts`, `database.ts` or `pg_description`, or column-comment coverage fell. Writes with `npm run agent-account`; `--record` moves the ratchet. With no database configured it prints a skip and exits 0 — the template's bundled sample is that path, and a check that always skipped would read as an answer. Fixture coverage of the renderers, the ratchet and the skip lives in `scripts/tests/agent-account.test.mjs`, which does run in CI. |

The `portable-core` CI job goes further than a diff: it applies the generated
core to a stock `postgres:17` with nothing in front of it, applies the recipe
on top, replays every migration in a second database and diffs the two
inventories, loads the seed onto a third and reads it back as `anon`
(`check:seed-load`) — then breaks the core on purpose and fails if either guard
stayed green. A guard nobody has watched fail is a guard nobody knows the shape
of.

**The three live guards, and who owns the question.** `check:target`,
`check:agent-account` and `check:deployment-seed-load` need something a CI
runner here does not have: a live project, or a second checkout. The decision
is recorded rather than deferred — **this repository does not own the live
target**, and no job here reaches one.

That is a statement about what a template is. This repository has no
deployment: any project it could be pointed at belongs to somebody else, and a
job here that reached one would be this package asking a question about a
database it does not run. A deployment has exactly one, knows when it changed,
and is where a red answer is actionable. So the live half of each of these runs
in a deployment, against that deployment's own target:

| check | who runs its live half | how it is kept running |
| --- | --- | --- |
| `check:target` | the deployment, whenever it points a checkout at a database, and after a deploy | the deployment's own live-check coverage |
| `check:agent-account --check` | the deployment, against the project whose account it generated | the same |
| `check:deployment-seed-load` | nobody, by decision — see below | — |

A deployment that takes this package is expected to declare its live checks and
to fail if one of them stops running, rather than to remember. The one this
package was generalised from does that; the mechanism is the deployment's, not
this repository's, and nothing here asserts it.

`check:deployment-seed-load` is the one where the decision costs something, and
it is worth saying plainly: it asks whether this package's portable core accepts
a **real** deployment's seed, which is the template's own question, not a
deployment's. Answering it needs two checkouts side by side. The decision is
that this repository does not take that on — so nothing runs it on a schedule,
and the question is answered only when somebody runs it by hand from a machine
that has a deployment checked out beside this one, before a release. Its sibling
`check:seed-load` runs in CI on every pull request and proves the loop closes on
content this repository generated; it cannot tell you the core is *sufficient*.

None of this is a silent skip. Each of the three writes to the unverified
register when it looks at nothing — a `::warning::` and a line in the run
summary, once per fact — so a green run says which question went unanswered
rather than implying it was answered. That register is the floor here, and the
floor is the answer: the warning is expected, and reading it is the whole
mechanism. What is not acceptable is adding any of the three to the `check`
job, where it would print a passing line over an absent subject.

(`check:sample-content`, in the next section, is outside CI for a different
reason — CI *could* run it, and what it would report every time is correct.)

The parts of all three that can be wrong without a database ARE held in CI:
`scripts/tests/deployment-seed-load.test.mjs` and
`scripts/tests/agent-account.test.mjs`.

## 4. The pipeline, and the claims about the repo itself

| Command | What goes red |
| --- | --- |
| `npm run test:ir` | The blueprint round trip broke: validation, seed SQL, the no-database fallback, or the schema-version migration that carries a signed-off blueprint forward instead of stranding it. Python standard library only. |
| `npm run check:standalone` | A file a commit would carry — tracked, or untracked and not ignored — names the deployment this template was generalised from. Word-bounded and case-insensitive for one term, case-sensitive for the other — the reasons are in the script's header, and relaxing either turns the check into noise that gets deleted. Mirrored content is excluded, because a mirror reports every sentence twice. |
| `npm run check:content-coupling` | A shared file carries that deployment's CONTENT rather than its name. The sweep above is a word-grep, so a cell id copied out of that database is thirty-two hex digits naming nothing, a lane actor from its cast is a job title, and one of its scenario names is an ordinary noun phrase — each as unusable to an adopter as that deployment's repository name in a comment, and each invisible to a grep for the name (#329). Four patterns, every one a SHAPE rather than a copy of somebody's catalogue: an opaque UUID, that deployment's cast, its scheduling vocabulary, and a `/touchpoint-logos/` asset path — spelled out, with the reason each is bounded the way it is, in the script's header. The id rule needs no list — the sample's own ids all come out of `fid()` in `scripts/generate_sample_blueprint.mjs`, so its `f0000000-…` prefix is a proof of origin, and a placeholder somebody typed has three or fewer distinct hex digits once the required version and variant nibbles are dropped, against the five and up a real deployment mints. Subject is everything a commit would carry, tracked and untracked, which is the subject `check:standalone` reads and the `commit` subject of `scripts/sweep.mjs` now states once for every check that reads it. It used to be seven directory prefixes no root-level file can match, so `AGENTS.md` — the always-loaded tier — was outside it along with every other root document, and a foreign cell id appended there passed in green. Tests are out, because a fixture has to be able to write the value down; `supabase/` is out, because its seed is generated and its migrations are history while the hand-written generator of both is in. `ALLOWED` holds a site that cannot move without a design decision, with that decision as its `why`, and an entry nothing matches any more is itself a failure. |
| `npm run check:sample-content` | *Not in CI, and it never goes red — it reports and exits 0.* The mirror of the two rows above, and the only check in this document that faces an ADOPTER rather than this repository: the sweeps above ask whether the template still carries the deployment it was generalised from, and nothing asked the other question — is what this deployment serves still the template's blueprint rather than its own? Three markers over the meta-blueprint (the service blueprint of this template itself): its service name `Keeping a blueprint true`, its `f0000000-…` id namespace — the marker with the name filed off, thirty-two hex digits that let content read as entirely an adopter's while still being keyed on the template's rows, and the same prefix `check:content-coupling` trusts as proof of origin, imported from that script rather than copied — and its six scenario titles. Subject is only the two places a deployment's CONTENT lives: the seed `[db.seed]` names (read with `resolveSeedFiles`, so a renamed or split seed is still swept) and `src/data/`, the board the app renders with no database. `scripts/generate_sample_blueprint.mjs`, this document and `README.md` are out — each names the sample and always will, and a sweep including them could never reach zero on any deployment. It is ADVISORY because both states it reports are legitimate: a fresh clone ships the sample on purpose (SETUP.md § 2) and a half-migrated deployment is a supported place to stand, so failing either would teach its reader to skip build output. A fresh clone reports ~2,700 sites; a deployment that has put its own content in reports none. Groups by marker and file with named example sites and `--all` for the rest. |
| `npm run check:docs-index` | `INDEX.md` or `docs/index.md` is stale, or a document under `docs/` has no `summary:` in its frontmatter. The failure names the file. Run `npm run docs:index`. |
| `bash scripts/tests/resource-split-migration.test.sh` | A populated replay of `21000113000000` lost the target of a legacy citation while moving it from `cells.links` into the `ref` column `evidence` carried at the time. It applies that one migration and stops, so it still reads a column `21000208000000` has since dropped — the assertion is about what that migration did on the day, not about the table's shape today. Requires a local PostgreSQL server; CI runs it in the portable-core job. |
| `bash scripts/tests/evidence-note-migration.test.sh` | A populated replay of `21000208000000` destroyed prose or dropped a column it was told to refuse over. The empty CI replay proves the file parses; every claim that matters here is about rows — an excerpt reaches the note, a surviving reference stops the migration, and an excerpt beside a different note stops it too. It applies the file with `psql -1`, because a guard that raises only means something when the fold rolls back with it. Requires a local PostgreSQL server; CI runs it in the portable-core job. |
| `bash scripts/tests/entity-examples-round-trip.test.sh` | An example authored in a blueprint source did not reach `services.entity_examples`, or a re-map erased one the deployment had. The column is written from the editor too, so what the emitter produces is only half the claim — this loads a generated seed onto a replayed schema and reads the row back, including the case the conflict clause exists for: a source that authors no examples must leave the target's alone. Requires a local PostgreSQL server; CI runs it in the portable-core job. |
| `npm run check:render-walk` | The app does not render. Chromium over the built distribution, previewed in no-database mode, walking the bundled sample board exhaustively — every phase, every scenario, every path one at a time, and every layout that scenario offers (merged only where two or more paths make it a different board) — and failing on any console error or page error, named with the address it appeared on, or on a board that arrives without lanes, without step headers or with empty cells. It is the only guard here that opens a browser, and the reason it exists is docs/adr/0017-large-component-splits-wait-for-an-end-to-end-round.md: a renamed at-rule whose whole block the browser dropped, and a lane chip that set `backgroundColor` to a role key rather than a colour, both shipped past a green build. It does NOT see a wrong colour — a chip that lost its tint renders — so it files one screenshot per view and CI uploads them: the guard is the machine half, the screenshots are the human half. The inventory is read off the rendered page rather than imported from `src/data/`, because `render-walk/` is a published path a deployment runs against its OWN sample board (`render-walk/README.md`). Its CI job builds with the Supabase variables explicitly empty — `isSupabaseConfigured()` reads them at BUILD time — and runs the walk twice, first with `RENDER_WALK_INJECT_CONSOLE_ERROR=1` asserting it goes red, because a walk that reports no console errors looks identical to one whose listener was never wired up. Locally: build with those variables cleared, or the walk refuses the run it is given. It carries a second case beside the walk: `render-walk/annotation-drag.spec.ts` is the browser half of `npm run slice:annotation-drag` — the three things that slice stubs because jsdom cannot do them (layout, the canvas's live CSS-transform camera, pointer capture) — drawing a box across two cells of one lane with real mouse moves, dragging it onto a third, and asserting the captured cell ids out of the capture menu's own `Save N marks` download, which needed nothing added to the app to be observable. Both cases obey the console-error rule, self-test included; the two together took 35 seconds locally, 5 of them the drag. |
| `npm run lint`, `npm run build` | ESLint, then a typecheck and a production build. |

## 5. The router

`AGENTS.md` is the whole always-loaded tier: the one file a session is handed
before it decides anything, here and in a workspace scaffolded from here.
`scripts/always-loaded.mjs` is the single list of what is in that tier and, at
length, what is deliberately out — the plugin manifests included, because the
host reads those at install time rather than handing them to a session. All
three checks below read that one list, so a file added to the tier moves all
three at once.

| Command | What goes red |
| --- | --- |
| `npm run check:budget` | The always-loaded tier is over its stated char budget — or, just as loudly, so far under it that the budget has stopped describing the file. Every character of the router is paid for by every task, including the ones it has nothing to do with, so the ceiling is the price of the tier. It fails DOWNWARD too because a ceiling that only ever blocks is one nobody re-reads: the fix is a one-line edit to `BUDGET`, and it is what makes the number a promise rather than a decoration. The report names every file it counted and its size. |
| `npm run check:negation` | Prohibition tokens across the tier rose above `RECORDED`. Steering by prohibition drags the forbidden behaviour into context and makes it more available, not less; the fix is to state the target behaviour. It is a ratchet, not a threshold — a threshold gets argued about and switched off, a ratchet only asks that the count fall — and a fall passes with a nudge to re-record. Quoted speech and code spans are exempt. A run measuring fewer files than `RECORDED.files` fails, because a corpus that lost a file would otherwise clear the ratchet every time with a smaller, greener number. |
| `npm run check:pointers` | A pointer in the router does not resolve, buries its trigger word, or is missing entirely. Three silent failures, one sweep: a renamed path leaves the agent loading a document that is not there and guessing; an item opening with "Any" makes the agent read filler before it learns which branch it is on; and an item naming no document at all is a body living in the router, prose every session pays for. § Rules that hold for every skill is exempt from the last two — those bind before any pointer could fire, which is why they are inline — and their pointers still have to resolve. |

The sweep is by structure rather than by a list of pointers, so a row added
tomorrow is swept tomorrow, and a section pointer (`path.md` § Heading) is held
against the heading as well as the file. `scripts/tests/the-router-is-a-router.test.mjs`
drives each rule from a router that breaks it — a padded tier, an added
prohibition, a dead pointer, a buried trigger, an item that names nothing —
against throwaway trees, so no failing branch needs a broken router committed
to prove it.

## 6. The glossary, and the map it stopped carrying

`CONTEXT.md` is the first pointer the router fires, so a session that reads
`AGENTS.md` reads this next. It was two thirds reference material until #137: a
rename map, an interface-to-schema map and a section of reasoning about which
words a sweep should skip, all paid for by every session that opened the file to
look up one word. The rename map is now `scripts/retired-vocabulary.mjs` and the
skip reasoning is the header of `scripts/check-retired-identifiers.mjs` — each
beside the code that acts on it — and the interface map is
`references/interface-schema-map.md`, one pointer away and generated.

| Command | What goes red |
| --- | --- |
| `npm run check:glossary` | `CONTEXT.md` holds something that is not a definition. Three rules, each naming a way the file grew last time: a fenced code block (a snippet or a listing is the beginning of a reference document), a table row naming a `table.column` (a table of column names restates the catalogue, which is what the generated map is for), and a `##` section with no `**term** — definition` row in it (a section that defines no word is a body, and all three evicted sections failed this on the day they were written). Tables as such stay allowed — the glossary draws one, of who writes what, and no cell of it names a column. |
| `npm run check:database-names` | A string literal in `src/`, `scripts/` or `skills/` names a database object the schema does not have. Three assertions, because the failures are different shapes. The first is the rename map: the argument to `.from(…)` / `.rpc(…)`, an embed hint inside a `.select(…)`, the same syntax in a raw PostgREST query string, and a `public.<name>` in SQL this repo GENERATES, each held against `scripts/retired-vocabulary.mjs`. The second reads `supabase/generated/portable-core.schema.sql`: a query PATH — `<relation>?select=<columns>` — names its own relation, so the relation and every column and embed inside it must be one the dump declares. That half is #173. `21000116000000` renamed the `findings` table to `audit_findings` and `.note` to `.summary`, and the map retires neither word on purpose — `finding` is the live domain word and `note` is live everywhere else — so `scripts/agent-harness/run.mjs` read a table that is not there for a release, alongside `slices.description`, `slices.origin` and `slice_items.caption` three lines above it. A retired name is still followed through the map, so a dead table and its dead column are reported from one site rather than in two rounds against a live database. The column half stops at the query path: a bare `.select('id, name')` keeps its relation on another line, and a check that guessed at it would fail correct calls. The third reads the dump too, for the other strings that carry their own relation — `insert into public.<table> (<columns>)` and `update public.<table> set <column> = …`, the SQL this repo generates. That half is #277: `slice_tools.py` inserted `slices.description` and `slices.origin` for a release, columns `21000116000000` renamed to `summary` and `authorship`, so every slice the skill imported was a statement Postgres rejects. Neither word is in the map and neither can be — both are live columns on other tables. Adjacent string literals are joined before the match, because a statement long enough to need two lines keeps its relation and its column list in different strings; a list assembled from a variable is dropped rather than guessed at. `skills/` is in the roots for this: the two shipped skill scripts are the most exposed code in the repository and no database-name guard walked them before. |
| `npm run check:pinned-shapes` | A row shape a document PINS for a model disagrees with the schema. `agents/auditor.md` tells an auditor exactly which keys to produce, and `audit_tools.py` validates against that shape — so when `21000116000000` made `check_name` into `check_key` and `note` into `summary`, the document went on asking for a row the validator raises `KeyError` on, and the failure read as the model being wrong. Two assertions: a key that is not a column of the bound relation, and a required column the shape never names. The binding is per fence and short on purpose — three of the four fenced blocks in `agents/`, `skills/` and `references/` document an agent's own output or a state file rather than a row, so treating every fenced key as a column would be wrong three times in four. Binding to a RELATION is also what makes `note` catchable: it is a live column on `paths`, `scenarios`, `cell_dependencies`, `evidence` and `service_account_emails`, and wrong only here. |
| `npm run check:interface-map` | `references/interface-schema-map.md` is not what its sources render. Its binding table comes from `LABEL_COLUMNS` in `scripts/interface-schema-map.mjs` and its coverage line from the `COMMENT ON` statements in `supabase/generated/portable-core.schema.sql`; a label bound to a name that dump does not have fails here too. Run `npm run interface-map`. The rename map's two halves needed a parity test because both were hand-kept; a generated half cannot disagree with its source, so what is left to check is the rendering — `scripts/tests/labels-name-their-columns.test.mjs` holds the emitted rows against `LABEL_COLUMNS`, which is a difference `--check` cannot see because `--check` compares the document to the same render. |

`scripts/tests/the-glossary-is-a-glossary.test.mjs` drives each rule from a
fixture that breaks it, and asserts the committed file last, as one case rather
than as the whole suite.

## 7. Adding one

A new guard belongs here when a claim in this repository is currently true and
nothing would notice if it stopped being. Four rules the existing set follows:

- **Fail by naming the file and the line.** A guard that says only "drift" is
  a guard someone has to reproduce before they can fix it.
- **Say what to run.** Every failure message above ends in a command.
- **Generate, then diff.** Where an artifact can be derived, derive it and
  diff it rather than hand-maintaining a second copy. A second hand-maintained
  copy is the drift surface the guard was meant to close.
- **Name a subject; judge only.** A check does not resolve a root from its own
  location, walk a directory, or decide what a file that vanished between the
  listing and the read means. It asks `scripts/sweep.mjs` for one of the
  subjects (`app`, `docs`, `scripts`, `migrations`, `references`,
  `reference-docs`, `deployment-seed`, and `commit` — everything a commit
  would carry), receives the files, and contains only
  its judgement — so a deployment that reads the application out of the
  package, with residents of its own laid over it, is swept where the build
  resolves it. Each subject states what "cannot see the subject" means there: a
  skip said out loud, or a failure. Its tests hand files in and test the
  judgement; the walk is tested once, in the sweep's own suite.
