## What this changes

<!-- The behaviour, not the diff. One concern per PR. -->

## Why

<!-- What was wrong, or what this makes possible. -->

## How it was checked

<!-- Which gates you ran, and what you looked at. For rendering changes:
     a screenshot or a measured before/after. "Looks right" is not
     checkable by a reviewer. -->

- [ ] `npm run typecheck` (`tsc -b` — a bare `tsc --noEmit` checks nothing here)
- [ ] `npm run lint`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] `bash scripts/tests/run_tests.sh` (if the blueprint pipeline changed)

## Notes for the reviewer

<!-- Anything you are unsure about, or deliberately left out of scope. -->
