---
'agentic-service-blueprinting': patch
---

A shared file names the catalog decision instead of numbering it, so
`useStakeholders.ts` can be byte-identical in a deployment that numbers the
same ADR differently.
