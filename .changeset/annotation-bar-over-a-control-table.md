---
'agentic-service-blueprinting': patch
---

The three annotation style bars and the three annotation marks are one bar and
one mark over a table of what each kind declares. A person styling, drawing and
dragging marks sees no change; the text mark's width floor, which was 80 in the
layer and 120 in the mark, is now one constant read in one place — the 120 the
screen already drew — so resizing a small text mark scales against the box on
screen.
