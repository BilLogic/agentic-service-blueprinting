---
'agentic-service-blueprinting': patch
---

Four comments in `sliceMutations.ts` said `origin` for the field a slice
actually carries, which is `authorship` — the rename that made a slice's
question "who wrote it" rather than "where it came from" moved the column and
left the sentences about it behind. A deployment gains comments that name a
field it can find.
