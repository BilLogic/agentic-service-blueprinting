---
'agentic-service-blueprinting': patch
---

Two shared files name the decision instead of citing an issue number. A deployment that enrols them in a drift gate no longer inherits a pointer to an issue its own tracker does not have.
